FROM ubuntu:18.04

WORKDIR /urweb
ENV COMPILER=/urweb/urweb-build

ENV URWEB_HOME=${COMPILER}
ENV LD_LIBRARY_PATH=${COMPILER}/lib
ENV PATH=${COMPILER}/bin:${PATH}

# serve requests
CMD ./memvalid.exe -q -k -t $((2 * $(nproc)))
